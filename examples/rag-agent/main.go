// rag-agent 演示带 RAG 能力的 agent。
// 完成 P1-P9 后，在这里组装一个能从文档中检索信息回答问题的 agent。
package main

import "fmt"

func main() {
	fmt.Println("RAG Agent Demo")
	fmt.Println("Prerequisites: P1-P3 (core) + P7-P9 (memory & retrieval)")
	fmt.Println("TODO: Index documents, wire up retrieval into context engine, run agent")
}
