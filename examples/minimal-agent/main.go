// minimal-agent 是阶段一完成后的里程碑 demo。
// 完成 P1 + P2 + P3 后，在这里组装一个能用工具回答问题的最小 agent。
package main

import "fmt"

func main() {
	fmt.Println("Minimal Agent Demo")
	fmt.Println("Prerequisites: P1 (LLM Gateway) + P2 (Tool Runtime) + P3 (ReAct Loop)")
	fmt.Println("TODO: Wire up LLM + Tools + Loop and run a simple task")
}
