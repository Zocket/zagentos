// mcp-demo 演示 MCP Server/Client 的交互。
// 完成 P4 + P5 后，在这里启动一个 MCP Server 并用 Client 调用它。
package main

import "fmt"

func main() {
	fmt.Println("MCP Demo")
	fmt.Println("Prerequisites: P4 (MCP Server SDK) + P5 (MCP Client & Registry)")
	fmt.Println("TODO: Start MCP server, connect client, list and call tools")
}
